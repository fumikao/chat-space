.side
  .side-header
    .side-header__name
      = current_user.name
    .side-header__icons
      = link_to new_group_path do
        = fa_icon 'edit'
      = link_to edit_user_path(current_user) do
        = fa_icon 'cog'
  %ul.side-groups
    %li.side-groups__group
      = link_to root_path do
        - current_user.groups.each do |group|
          %p.side-groups__group-name
            = group.name
          %p.side-groups__group-message
            まだメッセージはありません。